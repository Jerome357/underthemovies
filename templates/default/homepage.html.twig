{% extends 'base.html.twig' %}

{% block body%}
	<section class="jumbotron text-center">
		<div class="container">
			<h1 class="jumbotron-heading">Underthemovies</h1>
			<p class="lead text-muted">Les nouvelles citations</p>
			<p>
				<a href="{{path('quotes')}}" class="btn btn-secondary my-2">Toutes les quotes</a>
			</p>
		</div>
	</section>

	<div>
		<div class="col-md-10">
			<h2>Citation aléatoire</h2>
			<div>
				{{ render(controller('App\\Controller\\DefaultController::quote')) }}
			</div>
		</div>
	</div>
{% endblock %}
