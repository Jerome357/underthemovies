{% for quote in quotes %}


	<div class="card">
		<div class="card-header">
			<p>{{ quote.createdAt |date ('d-m-Y') }}</p>
			<p>{{ quote.user.username }}</p>
			<p></p>
		</div>
		<div class="card-body">
			<blockquote class="blockquote mb-0">
				<p>{{ quote.content }}</p>
				<p>
					{% for moviePersonage in quote.moviePersonages %}
                        {{ moviePersonage.personage.name }} - {{ moviePersonage.movie.title }}
                    {% endfor %}
				</p>
			</footer>
		</blockquote>
	</div>
</div></br>
{% endfor %}
